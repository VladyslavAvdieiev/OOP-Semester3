<Window x:Class="PresentationLayer.GraphicalUserInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer.GraphicalUserInterface"
        mc:Ignorable="d"
        Title="Restaurant" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Menu x:Name="Menu_Left" Grid.Column="0">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            
            <MenuItem x:Name="MenuItem_Ingredients" Header="Ingredient">
                <MenuItem.LayoutTransform>
                    <TransformGroup>
                        <RotateTransform Angle="90"/>
                    </TransformGroup>
                </MenuItem.LayoutTransform>
            </MenuItem>
            
            <MenuItem x:Name="MenuItem_Dishes" Header="Dishes">
                <MenuItem.LayoutTransform>
                    <TransformGroup>
                        <RotateTransform Angle="90"/>
                    </TransformGroup>
                </MenuItem.LayoutTransform>
            </MenuItem>
        </Menu>

        <ListBox x:Name="ListBox_Lists" Grid.Column="1" Visibility="Hidden" IsEnabled="False"/>

        <TabControl x:Name="TabControl_Files" Grid.Column="2">
            <TabItem Header="file1.xml">
                <Grid>

                </Grid>
            </TabItem>

        </TabControl>

    </Grid>
</Window>
